--- Double Pointers ---

class Solution {
    public ListNode reverseList(ListNode head) {
        ListNode prev = null;
        ListNode cur = head;
        ListNode temp = null;
        while (cur != null) {
            temp = cur.next;    // 1. 保存下一个节点
            cur.next = prev;    // 2. 当前节点指向前一个节点（反转核心）
            prev = cur;         // 3. prev 前进
            cur = temp;         // 4. cur 前进
        }
        return prev;
    }
}


--- Recursion ---
