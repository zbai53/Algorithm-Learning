class Solution {
    public void reverseString(char[] s) {
        int right = s.length - 1;
        int left = 0;
        while(right >= left){
            char n;
            n = s[left];
            s[left] = s[right];
            s[right] = n;
            right--;
            left++;
        }
        
        
    }
}
